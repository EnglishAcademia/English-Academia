---
layout: layouts/blog.njk
title: From the Margin
---

<div class="content-box">
  <header>
    <h1>From the Margin</h1>
    <p>A quiet corner for curious minds</p>
  </header>
</div>

<main>
  {% for post in collections.blog %}
  <div class="post-card">
    <a href="{{ post.url }}">
      <div class="post-title">{{ post.data.title }}</div>
      <div class="post-description">{{ post.data.description }}</div>
    </a>
  </div>
  {% else %}
  <p>No posts found.</p>
  {% endfor %}
</main>
